//! 思考vue怎么能获取到全局注册的组件呢？需要一个字典

const allComps = [];

const proVnode = () => {
    
};

const regComp = (vueOption) => {
  allComps.push({
    type: "xxxx",
    node: node,
  });
  console.log(vueOption);
};

export default regComp;
